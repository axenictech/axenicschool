<% unless @collections.nil? %>
	<%= t('Select fee collection date') %>
  <%= select :collection,:id, @collections.collect {|c| [c.name+" "+c.created_at.to_date.to_s,c.id]},{prompt:true},{onchange:"var a={id: $('#collection_id').val()};$.get('defaulter_students',a,function(response){});",class: "form-control"}%>
<%end%>