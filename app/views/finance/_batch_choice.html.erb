<% unless @batches.nil? %>
  <b>Select Batch:</b>
  <%= select :batch,:id, @batches.collect {|b| [b.course.course_name+" "+b.name,b.id]},{prompt:true},{onchange:"var a={id: $('#batch_id').val()};$.get('collection_choice',a,function(response){});",class: "form-control"}%>
<%end%>