<div>
  <ul class="breadcrumb">
    <li><%= link_to 'Home',home_dashboard_path %></li>
    <li><%= link_to 'Manage User',users_path %></li>
    <li>User Profile</li>
  </ul>
</div><div id="flash"><%= render 'layouts/messages'%></div>

<div class="row">
  <div class="col-md-12">
    <div class="pull-right">

      <% if @user.role.eql? "Student" or @user.role.eql? "Parent"%>
      <% unless @student.nil? %>
        <% if @student.status_description.nil? %>
          <%= link_to "View Profile",students_profile_path(@student), class: "btn btn-primary-link btn-xs" %>
        <% else %>
        <%= link_to "View Profile",students_archived_profile_path(@student.id), class: "btn btn-primary-link btn-xs"%>
        <%end%>
        <% end %>
      <% elsif @user.role.eql? "Employee"%>
        <% unless @employee.nil? %>
         <%= link_to "View Profile",employees_profile_path(@employee.id), class: "btn btn-primary-link btn-xs"%>
        <%end%>  
      <% end %>
      
      <%= link_to "Change Password", users_change_password_path(@user), class: "btn btn-primary-link btn-xs"%>
        <%= link_to "Edit", edit_user_path(@user), class: "btn btn-primary-link btn-xs"%>
        <%= link_to "Delete",@user,method: :delete,data:{confirm:"Are you sure to delete User: '#{@user.first_name+" "+@user.last_name}'?"}, class: "btn btn-primary-link btn-xs" %>
    </div>
  </div>
  </div>
<hr>

<div class="row">
 <div class="box col-md-2"></div>
  <div class="box col-md-6">
  <div class="box-inner">
    <div class="box-header well">
      <h2>User Profile</h2>
    </div>

    <div class="box-content">
    <fieldset>
  <div class="row">
    <div class="col-sm-7 col-sm-offset-1">
        <div class="form-group">
  
        <div class="col-md-6"><b> Username:</b></div>
          <div class="col-md-6"><%= @user.username %></div>
      
          </div>
          <br>
          <div class="form-group">
          <div class="col-md-6"><b> Name:</b></div>
          <div class="col-md-6"><%= @user.first_name+' '+@user.last_name  %></div>
          </div>
          <br>
          <div class="form-group">
          <div class="col-md-6"><b> Role:</b></div>
          <div class="col-md-6"><%= @user.role %></div>
          </div>
          <br>
              <div class="form-group">
          <div class="col-md-6"><b> Email:</b></div>
          <div class="col-md-6"><%= @user.email %></div>
          </div>
      </div>
 
 </fieldset>
 </div>
 </div>
 </div>
 </div>
 </div>


