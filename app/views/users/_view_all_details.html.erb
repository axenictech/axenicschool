<% if @users.nil? or @users.empty? %>
  <div align="center"><h4>No users found</h4></div>
<% else %>
  <div class="students-table">
    <table id="listing" align="center" width="60%" cellpadding="1" cellspacing="1">
      <tr class="tr-head">
        <td class="col-1">
          Sl no.
        </td>
        <td class="col-2">
          Name
        </td>
        <td class="col-3">
         Username
        </td>
        <td class="col-2">
        Role
        </td>
        <td></td>
         <tr class="tr-blank"></tr>
      <% @users.each_with_index do |user,i| %>
        <tr class="tr-<%= cycle('odd', 'even') %>">
          <td class="col-1">
            <%= i+1 %>
          </td>
          <td class="col-2">
      <%=  user.first_name + " " + user.last_name %>
          </td>
          <td class="col-3">
            <%= user.username %>
          </td>
          <td class="col-2">
          <%= user.role %>
          </td>
          <td >
            <%= link_to 'View Profile' ,user_path(user)%>
          </td>

        </tr>
      <% end %>
    </table>
  </div>
<% end %>