<% if @employee.nil? %>
 <h4 align="center">No Record Found </h4>
   <% else %>
<% if @employee.empty? %>
 <h4 align="center">No Record Found </h4>
   <% else %>
 <div class="col-md-1"></div>
      <table align="center" class="table table-hover" style="width:1000px">
      <tr>
            <th>Employee Name</th>
            <th>Employee Id</th>
            <th>Employee Department</th>
      </tr>

      <%@employee.each do |emp|%>
          <tr>
                <td col-md-2><%= link_to emp.full_name,employees_profile_path(emp)%></td>
                <td col-md-2><%= emp.id%></td>
                <td col-md-2><%= emp.employee_department.name%></td>
         </tr>
      <%end%>  
      </table>
  <% end %>
  <%end%>
