<div class="box-inner">
  
<div class="box-content">
      <table class="table table-striped">
      <tr>
            <th>Employee Name</th>
           
      </tr>

      <%@employees.each do |emp|%>
          <tr>
                <ul><td><%=link_to "#{emp.first_name} #{emp.last_name} (#{emp.employee_number})",
                employees_monthly_payslip_path(emp)%></td><ul>
         </tr>
      <%end%>  
      </table>
      </div>
      </div>