
  <%=form_for :leave, :url => {:action=>'update_department_leave_reset'},method: :get do |f| %>
<div class="row" align="right" style="margin-right:20px">
  <%= link_to "Assign All",employee_attendances_assign_all_path(@department.id),remote:true,class: 'btn btn-primary-link btn-xs'%>
  <%= link_to "Remove All",employee_attendances_remove_all_path(@department.id),remote:true, class: 'btn btn-primary-link btn-xs' %>
</div>
<div id="employee_list">
              <%= render 'employee_list' %>
        </div>
<div class="row">
  <div class="col-md-5"></div>
  <div class="col-md-5">
    <%= f.submit 'Reset', class: 'btn btn-primary'%>
  </div>
</div>
<% end %>
