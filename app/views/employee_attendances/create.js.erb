$("ul.errors1").html("")
<% if @attendance.errors.any? %>
		<%  @attendance.errors.full_messages.each do |message| %>
		$("ul.errors1").append($("<li />").html("<%= message.html_safe %>"))
		<% end %>
<% else %>
$('#employee_div').html("<%= j render 'employee'%>")
$("#myModal").modal("hide")
<% end %>